Here’s the Dart code for your calculator assignment based on that pseudocode:

```dart
import 'dart:io';

void main() {
  // Step 1: Define operation functions
  double add(double a, double b) => a + b;
  double subtract(double a, double b) => a - b;
  double multiply(double a, double b) => a * b;
  double divide(double a, double b) {
    if (b == 0) {
      print("Error: Cannot divide by zero.");
      exit(0);
    }
    return a / b;
  }

  // Step 2: Map operations to functions
  var operations = {
    '+': add,
    '-': subtract,
    '*': multiply,
    '/': divide,
  };

  // Step 3: Get user input
  stdout.write("Enter the first number: ");
  double firstNumber = double.parse(stdin.readLineSync()!);

  stdout.write("Enter the second number: ");
  double secondNumber = double.parse(stdin.readLineSync()!);

  stdout.write("Enter the operation (+, -, *, /): ");
  String operation = stdin.readLineSync()!;

  // Step 4: Perform the operation
  if (!operations.containsKey(operation)) {
    print("Invalid operation.");
    return;
  }

  double result = operations[operation]!(firstNumber, secondNumber);

  // Step 5: Display the result
  print("The result of firstNumberoperation secondNumber isresult");
}
```
